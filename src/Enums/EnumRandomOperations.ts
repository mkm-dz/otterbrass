
/**
 * Represents the possible operations that the random method can do.
 */
export enum EnumRandomOperations {
    None = 0,
    Add = 1,
    Remove = 2,
    GetChannelRandomness = 3,
    SetChannelRandomness = 4
}
